import Header from "@/components/Header";

export default function LoginPage() {
    return (
      <main>
        <Header/>
        <h1>로그인</h1>
        <button>구글로 로그인</button>
      </main>
    );
  }


